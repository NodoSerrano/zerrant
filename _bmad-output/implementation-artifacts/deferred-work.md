# Deferred work

## Deferred from: code review of 3-2-onboarding-step-1-ui-and-photo (2026-07-25)

- `updateProfile` y `saveOnboardingStep2` construyen el update sin trim y sin la guarda de `nombre_visible` que se agregó en `saveOnboardingStep1`. Si un form deja de mandar el campo, se manda `null` a una columna NOT NULL. Pre-existente. [`src/features/profile/actions.ts:203`]
- ~~El perfil se consulta dos veces por navegación al onboarding (proxy + server component) y el gate agrega una query a cada request protegido, incluidos los prefetch de `<Link>`.~~ **Addressed ZER-54** — gate movido a templates `(app)`/`(modal)` + `React.cache`; proxy sin `profiles` select en GET. Prefetch de `<Link>` ya no pega a DB en middleware; el costo queda en RSC template al navegar. Residual: POST stale a `/onboarding` sigue chequeando `onboarding_completado_en` en proxy (ZER-61).
- ~~La migración de grants arregla `profiles` y `tasks` pero no evita la recurrencia: la próxima tabla nace sin privilegios para `authenticated` y vuelve el `42501`. Falta `alter default privileges` o un chequeo automatizado.~~ **Done in ZER-49** — `ALTER DEFAULT PRIVILEGES FOR ROLE postgres` + `pnpm db:check-grants` en CI + probe de tabla nueva. [`supabase/migrations/20260919011500_zer49_default_privileges_for_role.sql`]
- ~~El prefill del paso 1 descarta el error de `.single()`: si la lectura falla, el usuario ve el formulario vacío sin explicación y reescribe datos que ya existen.~~ **Done in ZER-56** — fallos reales de lectura muestran alert + Reintentar a `/onboarding/step1`; `PGRST116` / perfil vacío siguen con form vacío; happy path precarga igual (vía `getOnboardingGateProfile`). [`src/app/(app)/onboarding/step1/page.tsx`]
- ~~Con el onboarding ya cerrado, un POST de server action desde una pestaña vieja parada en `/onboarding` recibe un 307 a `/` (que preserva método y body) en vez de un mensaje.~~ **Done in ZER-61** — POST/mutations a `/onboarding` con onboarding cerrado responden 409 HTML; GET lo resuelve el gate RSC (ZER-54). [`src/proxy.ts`]
- ~~`next/image` lanza si `profiles.avatar_url` apunta a un host que no está en `remotePatterns` (cambio de proyecto Supabase, dato migrado, build sin `NEXT_PUBLIC_SUPABASE_URL`). Rompe el render de la pantalla que el gate obliga a completar.~~ **Done in ZER-60** — `isServableAvatarImageUrl` + fallback en `AvatarPicker`. **ZER-68** movió el mismo guard (+ `onError` → initials) al `Avatar` compartido usado en `/profile`, plantel y `RequestCard`.

## Deferred from: story 4-5-create-task-4-2 (2026-07-27)

- ~~El wrapper de Pencil `ZqSLW` (frame `V0ODk`) pide padding `[6, 20, 24, 20]` (top 6 / laterales 20 / bottom 24), pero `(app)/layout.tsx` aplica un `p-5` uniforme.~~ **Done in ZER-52** — shells por route group: `(app)` hubs usan `pt-2 px-5 pb-5` = `[8,20,20,20]` (NgZiI / k8Odh / v9SljB); `(modal)` focused task screens usan `pt-1.5 px-5 pb-6` = `[6,20,24,20]` (ZqSLW / KG95R / vPUkG). Páginas que sumaban padding local encima del shell lo dejaron de hacer para no doblar. Outliers Pencil con laterales 24/28 (onboarding, solicitar, enviado, auth) siguen en `(app)` o fuera de estos shells y aceptan el padding del grupo hasta un route group propio.
- ~~El frame `V0ODk` muestra `StatusBar` y **no** muestra TabBar: la pantalla de crear tarea está diseñada como modal.~~ **Done in ZER-51** — create/detail/edit viven en `(modal)` (StatusBar, sin TabBar); hub y demás destinos quedan en `(app)` con TabBar. URLs públicas sin cambio.
- El componente `Input` agrega un `*` coral al lado del label cuando el campo es `required`, pero el componente `D7fHHd` de Pencil no modela estado requerido: el caption es sólo el texto. Es comportamiento del DS que viene de la story 1.2 (se ve igual en Email/Contraseña del login), no algo que introdujo ZER-21, y marcar los campos obligatorios es lo correcto en accesibilidad. Decidir si el `.pen` incorpora el asterisco o si el DS lo saca es una decisión de diseño que toca todos los formularios de la app. [`src/components/Input.tsx:22`]
- ~~`pnpm install` escribe un bloque `allowBuilds` de placeholders (`lefthook: set this to true or false`) en `pnpm-workspace.yaml`. No es config válida: es un stub que pnpm espera que alguien complete. Mientras nadie lo resuelva, cada `pnpm install` vuelve a ensuciar el árbol y hay que revertirlo a mano. Completarlo (o correr `pnpm approve-builds`) arreglaría de raíz el `ERR_PNPM_IGNORED_BUILDS` que obliga a commitear con `--no-verify`.~~ **Done in ZER-57** — `pnpm-workspace.yaml` ya trae `allowBuilds.lefthook: true` / `sharp: true` (desde `558cbee`); install limpio y clon fresco dejan el árbol limpio, sin `ERR_PNPM_IGNORED_BUILDS`, y `prepare` reinstala hooks lefthook. [`pnpm-workspace.yaml:1`]

## Deferred from: code review of 4-5-create-task-4-2 (2026-07-27)

- `createTask` no valida nada en runtime: `formData.get("titulo") as string` es un cast de TypeScript, no un chequeo. `titulo text not null` acepta `""`, así que el único guard contra una tarea sin título es el `required` del HTML. Además devuelve `error.message` crudo de Postgres al banner del form. Es el outlier del proyecto: `src/features/profile/actions.ts` sí valida, hace `trim()` y usa una constante `DB_ERROR` para no filtrar mensajes internos. [`src/features/tasks/actions.ts:29`]
- El `textarea` siempre manda `descripcion` en el FormData, así que una descripción vacía se persiste como `""` y nunca como `null`. La columna es nullable. Las pantallas que quieran distinguir "sin descripción" de "descripción vacía" (detalle de tarea, 4.4) van a ver los dos casos iguales. [`src/features/tasks/actions.ts:31`]
- ~~Un doble click muy rápido puede encolar dos invocaciones del server action antes de que `pending` deshabilite el botón, y no hay idempotencia en el insert. Es el patrón `useActionState` + `disabled={pending}` de toda la app (login, onboarding), no algo de esta pantalla. Se arregla de una sola vez o no se arregla.~~ **Done in ZER-59** — `useGuardedActionState` locks on the first submit across server-action forms; shared hook + tests.
- ~~Sin JavaScript no se puede navegar en ninguna parte de la app: la `TabBar` usa `<button onClick>` en vez de links.~~ **Done in ZER-50** — destinos built son `<Link>` reales. **ZER-67** sacó `INICIO` mientras `/` solo redirige a `/profile`; quedan `PLANTEL` · `NODO` · `AGENDA` · `PERFIL` (`/plantel`, `/nodo/tasks`, `/agenda`, `/profile`). Stub de `/agenda` hasta M6; reintroducir Inicio es del milestone 2.1.
- El literal `"tourist"` está hardcodeado por separado en la guarda del server component y en la de `createTask`. Si se agrega un tercer tier que no publica, o se renombra el actual, es fácil actualizar una y olvidar la otra — justo el bypass que la doble guarda existe para evitar. Falta una constante o helper compartido. [`src/app/(app)/nodo/tasks/new/page.tsx:27`]

## Deferred from: code review of 4-4-task-detail-4-1 (2026-07-29)

- **Quien toma una tarea puede escribir `estado` directo y auto-verificarse.** La policy `"Serranos can update tasks they took"` habilita al `creado_por` **o** al `tomada_por`, y el grant por columna incluye `estado`. O sea que el tomador puede setear `estado = 'verificada'` con un PATCH a PostgREST, salteándose por completo el chequeo de `is_platform_admin` que hace `verifyTask`. Lo mismo aplica a `titulo` y `descripcion`: puede reescribir el texto de la tarea que tomó. ZER-22 tapó el camino de la UI poniendo el filtro por `creado_por` en `updateTask`, pero el agujero de fondo sigue abierto y ahora el enum tiene un valor más que se puede escribir así. Arreglarlo pide separar la policy en dos (una para el creador sobre las columnas de contenido, otra para el tomador sobre `estado`/`tomada_por` y sólo en las transiciones válidas), o mover las transiciones a funciones `security definer`. [`supabase/migrations/20260725160000_grants_por_columna.sql:52`, `supabase/migrations/20260721232125_tasks.sql:38`]
- **`takeTask`, `markTaskDone` y `verifyTask` reportan éxito cuando no actualizan ninguna fila.** PostgREST no considera error que un UPDATE no matchee nada, y ninguna de las tres hace `.select()`, así que redirigen como si hubieran funcionado. Se ve, por ejemplo, cuando dos personas tocan "Tomar" a la vez: la segunda recibe la pantalla de éxito y la tarea queda a nombre de la primera. Es preexistente; ZER-22 arregla la forma sólo en las dos acciones que agregó. [`src/features/tasks/actions.ts:60`, `:104`, `:132`]
- **Una descripción de sólo espacios cambia de forma sola.** `createTask` persiste el textarea crudo, así que `"   "` llega a la base. El detalle la esconde (`descripcion?.trim()`), pero el form de editar la precarga invisible y `updateTask` la normaliza a `null` en el próximo guardado, aunque el usuario nunca haya tocado ese campo. Se arregla en `createTask`, que es donde nace. [`src/features/tasks/actions.ts:41`]
- **El hub no excluye las tareas canceladas de la lista por defecto.** Se decidió agregar la pill "Cancelada" y dejarlas en "Todas". Con el tiempo la lista por defecto se va a llenar de tareas muertas que ya no se pueden tomar. Excluirlas de "Todas" es una decisión de producto aparte. [`src/app/(app)/nodo/tasks/page.tsx:74`]

## Deferred from: code review of spec-zer-66-remove-fake-statusbar (2026-09-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-zer-66-remove-fake-statusbar.md`
  summary: Los worktrees paralelos `.claude/worktrees/zer-18-4-1-tasks-6c8409/` y `.claude/worktrees/zer-22-4-4-tasks-2c8290/` tienen copias intactas de `StatusBar.tsx`/`.test.tsx` y de los layouts que lo renderizan; van a chocar en merge/rebase contra `(app)/layout.tsx` y `(modal)/layout.tsx` una vez que ZER-66 se mergee.
  evidence: Confirmado por el Blind Hunter revisando el diff de ZER-66 contra el árbol de trabajo; es coordinación entre stories en curso de otros compañeros, no algo que ZER-66 deba resolver.
- source_spec: `_bmad-output/implementation-artifacts/spec-zer-66-remove-fake-statusbar.md`
  summary: Varias story files de otros compañeros (`4-5-create-task-4-2.md:289`, `4-1-plantel-list-and-filters.md:183`, `2-1-login-and-signup-brand-layout-1-1.md:127`) instruyen "no modificar StatusBar", que ahora es un archivo borrado.
  evidence: Son registros SSOT de stories ya implementadas/en curso de otras personas; corregirlas unilateralmente viola la regla de no tocar archivos de otros issues sin avisar.
- source_spec: `_bmad-output/implementation-artifacts/spec-zer-66-remove-fake-statusbar.md`
  summary: `docs/superpowers/specs/2026-07-20-nodo-serrano-backoffice-design.md` (líneas 297, 323) sigue contando `StatusBar` en el inventario de componentes reutilizables y en el total de "37 pantallas + component library".
  evidence: Es un documento de spec grande y congelado; ajustar el conteo de componentes es una decisión de alcance del documento, no una corrección mecánica de una línea.
- source_spec: `_bmad-output/implementation-artifacts/spec-zer-66-remove-fake-statusbar.md`
  summary: El borrado de `StatusBar` no se verificó en vivo contra `design/nodo-serrano.pen` (frames `UFbiI`/`V0ODk` y similares) porque el MCP de Pencil no estaba disponible durante la planificación ni la implementación.
  evidence: El spec ya marcaba esto como riesgo "Ask First"; el chequeo visual manual a 375px (captura del usuario) confirmó que el padding de los wrappers quedó correcto sin el StatusBar, pero la fuente de verdad del diseño (`.pen`) en sí no se pudo re-consultar.
