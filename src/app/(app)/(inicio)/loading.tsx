import { InicioHubSkeleton } from "@/features/home/InicioHubSkeleton";

/** Next segment loading for Inicio primary fetches (FR58). URL stays `/`. */
export default function InicioLoading() {
  return <InicioHubSkeleton />;
}
